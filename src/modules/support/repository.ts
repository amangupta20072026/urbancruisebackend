/**
 * support — repository (STUB)
 * TODO(step-2): SQL only, parameterized (?) queries.
 * EVERY query MUST filter by identity.entityId against the tenant column
 * (customer_id / vendor_id / driver_id / uc_user_id).
 * See ADR 0003 — tenant scoping is the last line of defense.
 */
export {};
