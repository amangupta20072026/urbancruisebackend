/**
 * customer.feedback — routes (STUB)
 * TODO(step-2): implement Express Router for this sub-module.
 * Each route mounts: authenticate → authorize(action, subject) → validate(schemas) → controller.
 */
import { Router } from 'express';
const router = Router();
export default router;
