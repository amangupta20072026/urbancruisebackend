/**
 * uc.performance — visibility (STUB)
 * TODO(step-2): isVisible(identity, subject) and redact(identity, subject).
 * Field-level gate — e.g. hide vendor commission from customer, hide
 * corporate admin PII from booking-person.
 */
export {};
