/**
 * uc.dashboard — service (STUB)
 * TODO(step-2): business logic. Loads subject via repo, runs
 * policy.canOperate(identity, subject) + visibility.isVisible/redact,
 * orchestrates repo calls.
 */
export {};
